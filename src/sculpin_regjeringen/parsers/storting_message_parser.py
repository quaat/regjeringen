"""Storting message parser placeholder."""

from __future__ import annotations


class StortingMessageParser:
    def parse(self, html: str, *, source_url: str, source_artifact_uri: str) -> object:
        raise NotImplementedError("Storting message parsing is planned after the hearing MVP.")
