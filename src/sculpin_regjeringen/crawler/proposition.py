"""Proposition category discovery placeholder."""

from __future__ import annotations


class PropositionCrawler:
    async def discover(self, crawl_batch_id: str) -> list[object]:
        raise NotImplementedError("Proposition discovery is planned after the hearing MVP.")
