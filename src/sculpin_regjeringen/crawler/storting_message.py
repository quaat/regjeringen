"""Storting message category discovery placeholder."""

from __future__ import annotations


class StortingMessageCrawler:
    async def discover(self, crawl_batch_id: str) -> list[object]:
        raise NotImplementedError("Storting message discovery is planned after the hearing MVP.")
